# Per-user WiFi credentials — created on the SD card (or by the setup page).
# Never commit real credentials; the template wifi.txt.example is tracked instead.
wifi.txt

# Local flash backups (e.g. from `esptool read-flash`)
*_backup.bin
stock_backup.bin

# Arduino / build output
build/
*.elf
*.map
